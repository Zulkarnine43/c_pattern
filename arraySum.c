#include<stdio.h>
int main()
{
int n, a,b,sum;
printf("enter total number:");
scanf("%d",&n);
for(int i=1;i<=n;i++){
        printf("Enter there number:");
    scanf("%d",&a);

}
sum=sum+a;
printf(" Sum is %d", sum);

return 0;
}
